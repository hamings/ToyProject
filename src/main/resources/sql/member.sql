CREATE TABLE MEMBER(
                       ID VARCHAR(30) PRIMARY KEY ,
                       NAME VARCHAR(30) NOT NULL,
                       EMAIL VARCHAR(50) ,
                       PHONE VARCHAR(30)
);